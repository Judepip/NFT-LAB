meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  command: python main.py
  persistenceMount: /data
  containerPort: 5000